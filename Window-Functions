SELECT DEPT, SALARY,
ROW_NUMBER() OVER (PARTITION BY DEPT ORDER BY SALARY DESC) AS RN_SAL,
RANK() OVER (PARTITION BY DEPT ORDER BY SALARY DESC) AS R_SAL,
DENSE_RANK() OVER (PARTITION BY DEPT ORDER BY SALARY DESC) AS DR_SAL,
FROM EMP
WHERE SALARY IS NOT NULL;


